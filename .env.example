# OpenAI 兼容服务配置（必填）
OPENAI_API_KEY=
OPENAI_MODEL=
# 使用兼容 OpenAI API 的服务时填写；官方 API 可留空
OPENAI_BASE_URL=

# BCS API（可选，不填写则使用内置 Mock）；base 填 host，如 https://bcs.example.com
BCS_BASE_URL=
BCS_API_TOKEN=
# 默认跳过 TLS 校验（仅 dev）；生产设 false 关闭
BCS_INSECURE_SKIP_VERIFY=
