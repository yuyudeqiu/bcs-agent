# 开发约定

## 项目背景

BCS Agent 是面向 Blueking Container Service 的运维助手，当前使用 Go + Eino，提供终端流式对话和 BCS 查询工具。
近期优先补齐项目、集群的基础接口，再逐步通过 BCS 网关访问 Kubernetes API，增加集群操作能力。

项目处于早期阶段，架构和产品形态仍会调整。以下目录职责描述当前结构，不要求长期固定；工具归并方式及 YAML 配置等方案尚未确定，不提前实现。Kubernetes API 访问使用 `client-go`，经 BCS 网关转发。

## 协作方式

- 需求明确且沿用现有实现方式的改动，直接推进并完成必要验证。
- 引入新的第三方库、技术组件或配置项前，先向用户同步用途、必要性、替代方案和影响，不等到完成后才告知。
- 新配置需说明名称、用途、默认值、是否必填及对现有行为的影响，优先考虑是否能复用已有配置。
- 调整模块职责、工具名称或参数结构，以及设计运维写操作的执行机制时，先同步方案。
- 已讨论确定的方案不重复征求确认；实施中出现新的选择或变化时再同步。
- 改动聚焦当前需求，避免顺带重构、提前抽象和为未确定的功能搭建框架。

## 当前代码职责

- `cmd/bcs-agent`：启动与组件组装。
- `internal/config`：配置读取；当前使用环境变量和 `.env`，这只是现有实现，不限制后续改用其他配置方式。
- `internal/agent`：模型、Agent 配置和运行时提示词。
- `internal/chat`：多轮历史和流式事件，保持与具体展示入口解耦。
- `internal/cli`：终端输入输出。
- `internal/tools/bcs`：工具描述、参数校验、调用客户端和结果序列化。
- `internal/bcs`：BCS Client 接口、数据类型、Mock 和真实 API 通信。
- `internal/kubernetes`：通过 BCS 网关访问目标集群的 Kubernetes API，以及对应的 Mock。

新增 API 通信逻辑放在客户端层，避免散落到 CLI、会话或工具适配中。工具粒度可以演进，不把“一种操作或一个 Kubernetes kind 对应一个工具”作为固定规范。

## 接口与工具约定

- 接口路径、HTTP 方法、鉴权、字段和分页行为以对应环境的接口文档或已验证响应为依据；信息缺失时明确指出，不猜测接口契约。
- 区分 BCS 管理接口与经网关代理的 Kubernetes API；接入时确认网关路径、目标集群和所需身份信息。
- 工具声明必填参数后，执行时仍需校验。对缺失参数或有歧义的项目、集群、命名空间及资源目标，不擅自选择。
- 分别处理 HTTP 错误、业务错误码和响应解析错误，保留有用的错误上下文，不把失败包装成成功或空结果。
- 列表接口确认分页语义；只获取部分结果时不能宣称返回了全部数据。
- 明确区分真实数据、Mock 数据和未实现能力。真实请求失败或接口未接入时，不静默回退到 Mock。
- 接口能力变化时，检查受影响的 Client、真实实现、Mock、工具定义与注册，保持相关部分一致。
- 请求传递 `context.Context`，遵循取消和超时；错误使用 `%w` 包装以保留原因。
- 凭证不硬编码、不提交、不输出到日志或工具结果；错误信息中的敏感内容需脱敏。
- 新增扩缩容、重启、删除等写操作时，应在执行流程中校验目标、影响与用户确认，不能仅依赖提示词约束。

## 验证与交付

- 修改 Go 代码后，对改动文件运行 `gofmt`，并运行 `go test ./...` 和 `go vet ./...`。仅修改文档时无需运行代码测试。
- 测试围绕实际行为：接口正常响应、参数校验及相关错误路径；会话改动关注流式事件、历史和失败处理。修复缺陷时按需补回归测试，避免只重复实现的测试。
- 默认使用 fake、Mock 和 `httptest` 验证，不依赖真实模型或 BCS 凭证；真实联调需明确目标环境和调用范围。
- 完成后简要说明改动、验证结果及尚未实现的部分。未运行或未通过的检查如实说明。

## 文档保持精简

- 不为普通功能额外创建设计文档、开发日志、总结或重复说明；需要新增独立文档时先同步其用途。
- 只有本次改动会使现有说明或配置示例产生误导时，才就地做必要更新，不将全面完善文档作为每次开发的附加任务。
- 本文件只记录已确认且可持续使用的约定；不作为待办清单，不固化尚未确定的技术方案。
